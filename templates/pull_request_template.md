## Summary

<!-- What changed and why. One paragraph. Focus on the why; the diff already shows the what. -->

## Test plan

<!-- How this PR was verified. Check what was actually run. Add additional
     smoke / manual verification checkboxes below if relevant. -->

- [ ] `<test command>` passes
- [ ] `<lint command>` clean

## Scope check

- [ ] No unrelated changes (diff matches the PR title)
- [ ] No hallucinated APIs (every imported symbol / called function exists)
- [ ] Existing behavior preserved, or behavior changes documented in Summary

## Specific risk

<!-- Performance, data migration, backward compatibility, security, secrets,
     external API surface, GPU determinism, anything reviewers should
     scrutinize. Write "None." if truly nothing. -->

---

<sub>For larger PRs, also write a full checklist report at
`docs/prs/<branch>-<YYYYMMDD_HHMMSS>.md` based on
`<workflow-repo>/templates/pr-checklist.md`.</sub>
